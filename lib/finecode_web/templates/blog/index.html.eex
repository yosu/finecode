<section class="row">
  <div class="container">
    <%= for post <- @posts do %>
    <div class="row">
      <h2><%= link(post.title, to: Routes.blog_path(@conn, :show, post.id)) %></h2>
      <p><%= post.description %></p>
    </div>
    <% end %>
  </div>
</section>
